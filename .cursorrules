你正在开发一个“文件体积膨胀工具”。

# 一、产品目标

构建一个基于 Web 的文件体积放大工具，允许用户上传指定格式的文件，在**不改变用户可见内容**的前提下，将文件体积膨胀到用户指定的倍数。

前端技术栈：
- HTML
- CSS
- JavaScript

后端技术栈：
- Python

目标支持的文件类型：
- .xlsx
- .doc
- .docx
- .pdf
- .ppt
- .pptx

系统必须优先保证以下目标：
1. 文件结构完整
2. 文件格式兼容
3. 可重复、可验证
4. 失败可控
5. 不破坏用户可见内容

---

# 二、核心功能要求

1. 系统必须支持浏览器上传文件。
2. 系统必须允许用户输入目标体积倍数，例如：
   - 1.5 倍
   - 2 倍
   - 3 倍
   - 5 倍
   - 自定义小数倍数
3. 系统默认必须生成新文件，不能直接覆盖原文件。
4. 系统必须尽可能保持原文件的可见内容和结构不变，包括但不限于：
   - Excel 单元格数据
   - 公式
   - 样式
   - 图片
   - 表格布局
   - Word 段落内容
   - PPT 页面内容
   - PDF 页面显示效果
5. 系统必须优先采用**标准兼容的元数据扩容方式**或**容器级安全扩容方式**。
6. 系统必须在处理完成后对输出文件进行验证。
7. 系统必须向用户提供处理后文件的下载能力。
8. 当某类文件无法在安全前提下达到目标倍数时，系统必须明确返回失败原因。

---

# 三、安全与兼容性原则

1. 未经明确要求，不得修改任何用户可见业务内容。
2. 不得为了达到目标体积而破坏文件结构。
3. 不得向未知位置盲目追加字节。
4. 必须优先采用格式标准允许的元数据膨胀方案。
5. 当目标倍数过高、无法安全实现时，必须安全失败，而不是输出可能损坏的文件。
6. 必须保持输出文件的扩展名和实际格式一致。
7. 未经过验证，不得声称“文件内容未变”或“文件可正常打开”。
8. 后端日志必须能够追踪每次处理所采用的策略与结果。

---

# 四、体积膨胀策略优先级

实现处理逻辑时，必须严格遵循以下优先级：

## 优先级 1：标准兼容的自定义元数据扩容
优先使用目标文件格式允许的：
- 自定义属性
- 文档属性
- 元数据对象
- 非渲染型扩展属性区
- 标准兼容的附加元数据容器

要求：
1. 必须是格式规范允许的区域。
2. 不得影响文件可见内容显示。
3. 不得影响主文档结构解析。
4. 必须通过后续验证。

## 优先级 2：容器级安全扩容
如果目标文件属于容器型格式，可使用不会影响文件显示和结构的安全扩容方式，例如：
- 容器内部安全元数据项扩增
- 低压缩率重新打包
- 不影响解析的标准兼容附加部件

要求：
1. 必须理解该容器格式的结构后再处理。
2. 不得把容器当成普通二进制文件盲改。
3. 处理后必须确保主流软件仍可正常打开。

## 优先级 3：格式特定的安全兜底方案
当元数据不足以达到目标倍数时，允许针对具体格式设计专用兜底方案，但必须满足：
1. 方案具备明确格式依据
2. 不影响用户可见内容
3. 可验证
4. 代码中必须写明风险、前提和适用范围

禁止使用“通用盲追加字节”的方式作为默认方案。

---

# 五、各文件类型处理规则

## 1. XLSX / DOCX / PPTX
这三类文件本质上属于 Office Open XML ZIP 容器。

实现要求：
1. 必须将其视为结构化包，而不是普通压缩文件。
2. 优先使用自定义文档属性、包级元数据、自定义 XML 部件等标准兼容方式。
3. 不得修改工作表正文、文档正文、幻灯片正文中的可见内容，除非明确要求。
4. 必须正确维护：
   - 关系文件
   - 内容类型声明
   - 包内路径结构
5. 如需重新打包，必须确保输出文件可被主流 Office 软件正常打开。
6. 对已有图片、样式、媒体资源，默认保持原样，不得无故改动。

## 2. DOC / PPT
这两类属于旧版二进制 Office 格式。

实现要求：
1. 默认视为高风险格式。
2. 不得进行无依据的二进制偏移修改。
3. 只有在存在明确、安全、可验证的元数据扩容策略时才允许支持。
4. 若当前阶段无法保证安全处理，必须明确返回：
   “该格式暂不支持安全的体积膨胀处理”
5. 不得伪装成已支持，不得输出未经验证的结果。

## 3. PDF
实现要求：
1. 必须保证页面视觉呈现不变。
2. 优先使用文档信息、元数据对象、增量更新兼容方案等安全手段。
3. 不得直接改写页面内容流，除非这是唯一安全方式且已明确验证。
4. 必须谨慎处理：
   - xref 表
   - 对象偏移
   - 增量更新结构
   - 线性化 PDF
   - 数字签名
5. 若 PDF 已签名，必须提示用户：任何修改都可能导致签名失效。
6. 输出后必须用解析器重新验证。

---

# 六、后端架构规范

后端采用 Python，必须使用清晰的分层架构。

推荐分层：
1. 上传接收层
2. 文件类型识别层
3. 策略选择层
4. 格式处理器层
5. 验证层
6. 下载响应层

推荐模块划分：
- app
- routes
- services
- processors
- validators
- models
- utils

架构要求：
1. 每种文件类型必须有独立处理器。
2. 目标体积计算逻辑必须放在公共服务层。
3. “文件修改”和“结果验证”必须分离。
4. 临时文件必须使用独立目录。
5. 所有文件操作必须使用安全路径。
6. 不能只根据扩展名判断格式，必须结合文件签名或结构判断。

---

# 七、前端实现规范

前端必须保持简单、清晰、以任务为中心。

页面必须包含以下区域：
1. 文件上传区域
2. 支持格式说明
3. 目标倍数输入区域
4. 处理状态展示区域
5. 结果下载区域
6. 错误提示区域

前端行为要求：
1. 提交前必须校验倍数输入是否合法。
2. 必须明确提示允许输入范围和格式。
3. 处理过程中必须禁用重复提交。
4. 必须展示清晰状态，例如：
   - 已上传
   - 校验中
   - 处理中
   - 验证中
   - 已完成
   - 处理失败
5. 后端未验证成功前，不得显示“处理成功”。
6. 界面风格应专业、简洁，避免无意义动画。
7. 必须满足基础可访问性要求：
   - 表单有标签
   - 键盘可操作
   - 对比度清晰
   - 状态文案明确

---

# 八、验证要求

所有输出文件都必须进行处理后验证。

验证目标：
1. 确认文件结构仍然可解析
2. 确认文件类型未发生错误变化
3. 确认输出体积达到或接近目标倍数
4. 确认文件可被对应解析器重新打开
5. 确认关键核心部件未丢失

按文件类型的最低验证要求：

## XLSX
- 工作簿可正常打开
- 工作表列表可读取
- 关键 XML 结构正常

## DOCX
- 文档包可正常打开
- 主文档部件存在
- 关系文件完整

## PPTX
- 演示文稿可正常打开
- 幻灯片列表可读取
- 包结构完整

## PDF
- 解析器可正常读取
- 页数保持稳定
- 文档对象结构有效

## DOC / PPT
- 仅当存在安全验证路径时才允许返回成功
- 否则必须返回不支持

如果验证失败：
1. 必须丢弃无效输出文件
2. 必须返回结构化错误信息
3. 不得将损坏文件作为成功结果交付用户

---

# 九、错误处理规范

必须返回准确、可读的错误信息。

可接受的错误分类包括：
- 不支持的文件类型
- 文件扩展名与实际格式不匹配
- 倍数输入非法
- 无可用的安全扩容策略
- 输出文件验证失败
- 请求的倍数过大，无法安全实现
- 文件已加密或受密码保护
- PDF 已签名，修改将破坏签名有效性

要求：
1. 面向用户的错误提示必须简洁明确。
2. 后端日志中可以记录详细诊断信息。
3. 不得把原始堆栈信息直接返回给前端用户。

---

# 十、安全要求

1. 必须限制上传文件大小。
2. 必须校验扩展名与文件签名。
3. 必须对文件名进行清洗。
4. 不得执行上传文件中的任何内容。
5. 不得仅信任浏览器上传的 MIME 类型。
6. 临时处理目录必须隔离。
7. 必须防止路径穿越。
8. 若对公网开放，必须增加限流策略。
9. 对异常结构包、损坏包、加密包应默认拒绝处理，除非已明确支持。
10. 含宏、活动内容或潜在执行载荷的格式，默认不处理，除非经过明确评估与支持。

---

# 十一、性能要求

1. 处理大文件时，应尽量避免一次性全部载入内存。
2. 在适合的情况下，应先做“可达目标评估”，再执行最终写入。
3. 尽量减少无意义的重复压缩和解压。
4. 相同输入和相同倍数下，处理结果应尽量可重复、可预测。
5. 必须及时清理临时资源。

---

# 十二、工程质量规范

1. 代码必须模块化、可读性强。
2. 优先使用明确逻辑，不要过度炫技。
3. 每个处理器必须明确说明：
   - 支持的格式
   - 扩容策略说明
   - 风险点
   - 验证方式
4. 必须记录结构化日志，至少包括：
   - 接收上传
   - 文件类型识别结果
   - 原始体积
   - 目标体积
   - 选用策略
   - 验证结果
5. 所有公共服务方法必须写 docstring。
6. 禁止魔法数字，阈值和限制必须常量化。
7. 必须编写单元测试，至少覆盖：
   - 目标体积计算
   - 策略选择
   - 非法文件拒绝
   - 正常验证通过路径
8. 必须编写集成测试，至少覆盖：
   - xlsx
   - docx
   - pptx
   - pdf

---

# 十三、产品行为要求

1. 原始上传文件必须保持不变。
2. 输出文件名必须体现处理结果，例如：
   - 原文件名_expanded_3x.xlsx
   - 原文件名_膨胀_3倍.pdf
3. 前端必须展示：
   - 原始文件大小
   - 输出文件大小
   - 实际达到的倍数
4. 如果无法精确达到目标倍数，但在安全范围内可接近实现，必须明确展示：
   - 用户请求倍数
   - 实际达到倍数
   - 偏差原因
5. 必须诚实展示结果，不能伪造“精确成功”。

---

# 十四、非目标与禁止事项

未经明确批准，不得实现或暗示支持以下用途：
1. 隐蔽性对抗
2. 规避平台限制
3. 绕过扫描、检测、审查机制
4. 恶意文件伪装
5. 对旧版二进制格式进行无依据补丁注入
6. 容忍损坏结果的文件生成

该工具的定位是：
**面向标准兼容前提下的文件处理与体积调节工具**
而不是任何规避型、对抗型或恶意用途工具。

---

# 十五、开发任务响应规范

当被要求实现、修改或扩展该项目时，必须按以下顺序思考和回答：

1. 先说明涉及的模块边界
2. 明确当前实现假设
3. 给出当前文件类型最安全的处理策略
4. 指出兼容性风险
5. 优先选择可测试、可验证的增量改动
6. 不得虚构“已支持”的格式能力
7. 当需求与安全性冲突时，优先保证文件完整性，而不是强行达到目标倍数

冲突优先级如下：
1. 文件完整性
2. 格式标准兼容
3. 验证正确性
4. 用户可见内容不变
5. 目标倍数精度

---

# 十六、默认技术假设

除非另有说明，默认认为：
1. 前端是浏览器页面
2. 后端通过 HTTP API 提供服务
3. 处理完成后通过下载接口返回文件
4. 默认采用同步处理；仅在文件过大或部署要求明确时才考虑异步任务
5. 优先保证代码可维护性，而不是过早优化

---

# 十七、完成标准（Definition of Done）

一个功能只有在满足以下条件时，才算真正完成：

1. 文件上传可用
2. 文件类型识别正确
3. 已选择安全策略
4. 成功生成输出文件
5. 输出文件验证通过
6. 输出体积数据展示准确
7. 用户可以下载处理后的文件
8. 失败路径明确且不会破坏原文件
9. 测试已覆盖新增行为
10. 没有对不支持的格式做虚假承诺