# 适配 Linux + Python 3.9.25 的依赖版本
# 目标环境: Linux x86_64 (kernel 5.14+), Python 3.9.25
# 安装命令:
#   pip install -r requirements-linux.txt
# 若网络受限，可使用国内镜像:
#   pip install -r requirements-linux.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ===== Web 框架 =====
# Python 3.9 支持 Flask 3.x，可使用更现代的版本
Flask>=3.0.0,<3.1.0
Werkzeug>=3.0.0,<3.1.0
Jinja2>=3.1.0,<3.2.0
itsdangerous>=2.1.0,<2.3.0
click>=8.1.0,<8.2.0
MarkupSafe>=2.1.0,<3.0.0

# ===== Office 文件处理库 =====
openpyxl>=3.1.2,<3.2.0
python-docx>=1.1.0,<1.2.0
python-pptx>=0.6.23,<1.1.0

# ===== PDF 处理库 =====
# Python 3.9 支持 pikepdf 8.x，性能更好，对 PDF 兼容性更强
# pikepdf 在 Linux 上依赖 libqpdf，通常系统需要安装:
#   Debian/Ubuntu: apt-get install libqpdf-dev
#   RHEL/CentOS/Rocky/AlmaLinux: dnf install qpdf qpdf-devel
#   不过 pikepdf 官方 wheel 已自带预编译 qpdf，一般无需手动安装
pikepdf>=8.0.0,<9.0.0
PyPDF2>=3.0.1,<4.0.0

# ===== 测试依赖 =====
pytest>=8.0.0,<9.0.0
pytest-flask>=1.3.0,<1.4.0
